# Wyoming Soundings

Downloads and cleans upper-air sounding text for a configured station and date
range.

```powershell
python -m pip install -r requirements.txt
python wyomingscrape.py
```

Output is written to `output/`; override it with `WYOMING_OUTPUT`.
